<header>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid mx-3">
          <a class="navbar-brand" href="/">DigitalMall</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link <%= path === 'home'? 'active' : ''%>" aria-current="page" href="/">Home</a>
              </li>
              <% if(isAthenticated && isAdmin) {  %>
              <li class="nav-item">
                <a class="nav-link <%= path === 'cart'? 'active' : ''%>" href="/cart">Cart</a>
              </li>
              
              <li class="nav-item">
                <a class="nav-link <%= path === 'order'? 'active' : ''%>" href="/order" >Order</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= path === 'adminproducts'? 'active' : ''%>" href="/admin/products" >Admin Products</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= path === 'admincategory'? 'active' : ''%>" href="/admin/catagory">Admin Category</a>
              </li>
            </ul>   
            <form class="d-flex" action="/logout" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button class="btn btn-primary mx-3" type="submit">Logout</button>    
            </form>
            <% } else if(isAthenticated){ %>
              <li class="nav-item">
                <a class="nav-link <%= path === 'cart'? 'active' : ''%>" href="/cart">Cart</a>
              </li>
              
              <li class="nav-item">
                <a class="nav-link <%= path === 'order'? 'active' : ''%>" href="/order">Order</a>
              </li>
            </ul>   
            <form class="d-flex" action="/logout" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button class="btn btn-primary mx-3" type="submit">Logout</button>    
            </form>
            <% } else { %>
              </ul>
              <div class="d-flex">
                <a class="btn btn-primary mx-3 <%= path === 'login'? 'active' : ''%>" href="/login">Login</a>
                <a class="btn btn-primary mx-3 <%= path === 'signup'? 'active' : ''%>" href="/signup">SignUp</a>
              </div>
            <% } %>  
          </div>
        </div>
      </nav>
</header>